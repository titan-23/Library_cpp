#include <iostream>
#include <cassert>
using namespace std;

// beam_search
namespace titan23 {

namespace beam_search {

  // TODO:
  enum Action {};
  ostream& operator<<(ostream& os, const Action &action) {
    switch (action) {
    } // os << 'U';
    return os;
  }
}
}
